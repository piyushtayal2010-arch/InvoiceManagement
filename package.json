{
  "name": "invoice-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npm run start:backend",
    "start:backend": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev"
  }
}
